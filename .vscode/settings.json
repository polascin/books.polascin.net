{
  // PHP executable paths are machine-specific — set them in Cursor User settings
  // (Windows: %APPDATA%\Cursor\User\settings.json), not in this shared workspace file.
  // Keys: php.validate.executablePath, phpserver.phpPath, fiveServer.php.executable
  "git.autofetch": true,
  "git.autofetchPeriod": 180,
  "git.enableSmartCommit": true,
  "git.postCommitCommand": "sync"
}
